# 🔗 ความสัมพันธ์และการเชื่อมโยงข้อมูลใน Order Form (Data Relationship & Logic Flow)

**วันที่สร้าง:** 2025-12-02
**Version:** V1.0
**ไฟล์ที่เกี่ยวข้อง:** `components/OrderForm.jsx`, `lib/mockData.js`

---

## 🎯 ภาพรวม (Overview)

ระบบ Order Form ประกอบด้วย 4 ส่วนหลักที่มีความสัมพันธ์กันแบบ **Hierarchical (ลำดับชั้น)** และ **Inheritance (การสืบทอดข้อมูล)** ดังนี้:

1. **ข้อมูลลูกค้า (Customer)** - *Root Data (ข้อมูลต้นทาง)*
2. **ข้อมูลใบกำกับภาษี (Tax Invoice)** - *Derived from Customer*
3. **ข้อมูลงานหลัก (Master Job)** - *Derived from Customer & Template for Items*
4. **รายการสินค้า (Order Items)** - *Inherits from Master Job*

---

## 1. ข้อมูลลูกค้า (Customer Card)

**สถานะ:** **Root (ต้นทาง)**
เป็นจุดเริ่มต้นของข้อมูลทั้งหมด การเปลี่ยนแปลงที่นี่จะส่งผลกระทบต่อส่วนอื่นๆ

* **การทำงาน (Action):** เมื่อผู้ใช้ค้นหาและเลือก "ลูกค้า"
* **ผลลัพธ์ (Effect):**
    1. **โหลดข้อมูลใบกำกับภาษี:** ดึงรายการ `taxInvoices` ของลูกค้านั้นมาเตรียมไว้
    2. **โหลดข้อมูลที่อยู่:** ดึงรายการ `savedAddresses` ของลูกค้านั้นมาเตรียมไว้สำหรับ Dropdown
    3. **Auto-fill:**
        * เลือกใบกำกับภาษีรายการแรก (Default) ลงใน **Tax Invoice Card** ทันที
        * (Optional) อาจจะเลือกที่อยู่แรกลงใน **Master Job Card** (ขึ้นอยู่กับ Logic ปัจจุบัน)

---

## 2. ข้อมูลใบกำกับภาษี (Tax Invoice Card)

**สถานะ:** **Independent Child (ลูกอิสระ)**
อ้างอิงข้อมูลจาก Customer แต่สามารถแก้ไขอิสระได้

* **ความเชื่อมโยง:**
  * **Dropdown:** แสดงรายการ `taxInvoices` จาก Customer ที่เลือก
* **Logic:**
  * ถ้าเลือกจาก Dropdown -> Auto-fill ข้อมูลบริษัท, ที่อยู่, เลขผู้เสียภาษี
  * ถ้าพิมพ์แก้ไขเอง -> ไม่กระทบข้อมูลต้นฉบับใน Database (จนกว่าจะบันทึก Order)
  * **ไม่ส่งผลกระทบ** ต่อ Master Job หรือ Order Items (เป็นข้อมูลสำหรับ Billing เท่านั้น)

---

## 3. ข้อมูลงานหลัก (Master Job Card)

**สถานะ:** **Template & Controller (แม่แบบและตัวควบคุม)**
เป็นหัวใจสำคัญในการกำหนดพฤติกรรมของรายการสินค้า (Order Items)

* **ความเชื่อมโยง:**
  * **Location Dropdown:** แสดงรายการ `savedAddresses` จาก Customer ที่เลือก
* **Logic สำคัญ:**
  * **Job Type Selection:**
    * `Installation` / `Delivery`: บังคับให้ลูก (Items) ทำตาม
    * `Separate Job`: ปล่อยให้ลูก (Items) อิสระ
  * **Data Entry:** ข้อมูลที่กรอกตรงนี้ (ทีม, วันที่, สถานที่) จะเป็น **ค่าเริ่มต้น (Default)** ให้กับ Order Items

---

## 4. รายการสินค้า & รายละเอียดงาน (Order Items & Job Details Popup)

**สถานะ:** **Inheritor (ผู้รับมรดก)**
รับข้อมูลมาจาก Master Job และ Customer

### 4.1 ความเชื่อมโยง (Relationships)

* **กับ Master Job:** รับค่า Type, Team, Date, Location มาเป็นค่าเริ่มต้น
* **กับ Customer:** ใช้ `savedAddresses` ใน Dropdown เลือกสถานที่ (กรณีเป็น Separate Job)

### 4.2 Logic การแสดงผลใน Popup (Job Details Modal)

เมื่อกดปุ่มแก้ไข (✏️) ที่รายการสินค้า ระบบจะทำงานดังนี้:

**Step 1: ตรวจสอบโหมด (Check Mode)**

* ดูค่า `jobInfo.jobType` จาก Master Job
  * ถ้าเป็น `Installation` หรือ `Delivery` -> **Lock Mode (แก้ไขไม่ได้)**
  * ถ้าเป็น `Separate Job` -> **Edit Mode (แก้ไขได้)**

**Step 2: เตรียมข้อมูลแสดงผล (Data Preparation)**
ระบบจะใช้ **Smart Merge Logic** เพื่อดึงข้อมูลมาแสดง:

| Field | Priority 1 (Specific Job) | Priority 2 (Master Job) | ความหมาย |
| :--- | :--- | :--- | :--- |
| **Type** | `item.specificJob.type` | `jobInfo.jobType` | ใช้ค่าของตัวเองก่อน ถ้าไม่มีให้ใช้ของ Master |
| **Team** | `item.specificJob.team` | `jobInfo.team` | " |
| **Date** | `item.specificJob.dateTime` | `jobInfo.appointmentDate` | " |
| **Location** | `item.specificJob.installLocationName` | `jobInfo.installLocationName` | " |
| **Address** | `item.specificJob.address` | `jobInfo.installAddress` | " |

* **ผลลัพธ์:** ผู้ใช้จะเห็นข้อมูลเต็มเสมอ แม้ยังไม่ได้กำหนดข้อมูลเฉพาะเจาะจงให้สินค้านั้น

**Step 3: การบันทึก (Saving)**

* เมื่อกด "บันทึก" ใน Modal:
  * ข้อมูลจะถูกบันทึกลงใน `item.specificJob` ของสินค้านั้นๆ
  * ไม่มีการเปลี่ยนแปลงข้อมูลใน Master Job (One-way flow)

---

## 📊 แผนภาพการไหลของข้อมูล (Data Flow Diagram)

```mermaid
graph TD
    C[Customer Selected] -->|Load Addresses| M_Dropdown[Master Job Dropdown]
    C -->|Load Addresses| I_Dropdown[Job Details Dropdown]
    C -->|Load Tax Info| T[Tax Invoice Card]

    M[Master Job Card] -->|Control Type| I[Order Items]
    M -->|Provide Default Values| I

    subgraph "Job Details Modal Logic"
        Check{Master Job Type?}
        Check -->|Install/Delivery| Lock[🔒 Read-only (Force Inherit)]
        Check -->|Separate Job| Edit[✏️ Editable (Independent)]
        
        Edit -->|Select Location| I_Dropdown
    end
```

---

## 💡 ตัวอย่างสถานการณ์ (Scenario Examples)

### Scenario A: งานเหมาติดตั้ง (Single Installation)

1. **Customer:** เลือก "คุณสมชาย"
2. **Master Job:** เลือก "งานติดตั้ง", ทีม "ช่าง A", วันที่ "12/12/2025", ที่อยู่ "บ้านคุณสมชาย"
3. **Order Items:** มีสินค้า 3 ชิ้น
    * กดดูรายละเอียดสินค้าชิ้นที่ 1 -> เห็นข้อมูล "ช่าง A, 12/12/2025, บ้านคุณสมชาย" (แก้ไขไม่ได้)
    * กดดูรายละเอียดสินค้าชิ้นที่ 2 -> เห็นข้อมูลเดียวกัน (แก้ไขไม่ได้)

### Scenario B: งานแยกส่ง (Separate Delivery)

1. **Customer:** เลือก "บริษัท ABC จำกัด" (มีหลายสาขา)
2. **Master Job:** เลือก "Job งานแยก (Separate Job)"
3. **Order Items:** มีสินค้า 2 ชิ้น
    * **ชิ้นที่ 1:** กดแก้ไข -> เลือก "สาขาสยาม", ทีม "จัดส่ง 1"
    * **ชิ้นที่ 2:** กดแก้ไข -> เลือก "สาขาลาดพร้าว", ทีม "จัดส่ง 2"
    * *ผลลัพธ์:* สินค้าแต่ละชิ้นมีข้อมูลการจัดส่งที่แตกต่างกันอย่างอิสระ

---

## 📝 สรุปความสำคัญ (Key Takeaways)

1. **Customer คือศูนย์กลาง:** ต้องเลือกลูกค้าก่อนเสมอ เพื่อให้มีข้อมูลที่อยู่และใบกำกับภาษี
2. **Master Job คือตัวคุมเกม:** การเลือก Type ใน Master Job เป็นตัวตัดสินว่า Job ย่อยจะ "ถูกบังคับ" หรือ "อิสระ"
3. **Smart Merge:** Modal จะฉลาดพอที่จะดึงข้อมูล Master มาแสดงให้เห็นทันที ไม่ปล่อยให้ช่องว่าง แม้จะเป็น Job แยกที่ยังไม่ได้กรอกข้อมูลก็ตาม
